language: android
android:
  components:
    - tools
    - platform-tools
    - build-tools-28.0.3
    - android-28
before_install:
  - yes | sdkmanager "platforms;android-28"
jdk:
  - oraclejdk8
before_script:
  - chmod +x gradlew
script:
  - ./gradlew test
  - ./gradlew assembleDebug
notifications:
  email: false
